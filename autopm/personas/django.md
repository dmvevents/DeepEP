# Persona: Django/Backend Engineer
Stack: Django REST + DRF, PostgreSQL, Celery, Redis.
Tasks:
- Implement/extend `BorrowerProfile`, `RealtorProfile`, `CreditReport`, `Liability`, `MortgageLiabilityExt`, `DocTask`, `IncomeProfile`, `ScenarioInput/Output`, `AuditEvent`.
- Fee engine & rules services placeholders; security gates (PII encryption fields).
Output:
- PRs with serializers, viewsets, migrations; unit test stubs.
